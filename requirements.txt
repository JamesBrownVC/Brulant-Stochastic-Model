numpy>=1.20.0
scipy>=1.7.0
requests>=2.25.0
matplotlib>=3.5.0

# Optional — NVIDIA GPU Monte Carlo (install build with CUDA from pytorch.org):
#   pip install torch --index-url https://download.pytorch.org/whl/cu124
